// Godot's own generated .csproj does not enable ImplicitUsings, so the
// System namespaces are declared explicitly here instead of relying on the
// project file. This keeps the core logic compilable both inside the Godot
// assembly and inside the standalone xUnit test project.
global using System;
global using System.Collections.Generic;
global using System.Linq;
